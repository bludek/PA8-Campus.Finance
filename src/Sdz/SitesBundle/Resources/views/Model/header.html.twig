<div class="navbar-inner">
    <div class="container-fluid">
        
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </a>
        <a class="brand" href="{{ url('view_page',{'user': app.user.username, 'site': header.site.url, 'name':'accueil'}) }}">{{ header }}</a>
        <div class="nav-collapse">
            <ul class="nav">
                {% for link in header.links %}
                    <li><a href="{% if link.page %}{{ url('view_page',{'user': app.user.username, 'site': header.site.url, 'name':'accueil'}) }}{% else %}#{% endif %}">{{ link }}</a></li>
                {% endfor %}
            </ul>
            <!--<p class="navbar-text pull-right">Logged in as <a href="#">username</a></p>-->
        </div>
    </div>
</div>